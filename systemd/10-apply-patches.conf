[Service]
# Self-heal all ccgram source patches on every start (survives `uv tool upgrade
# ccgram`). Leading '-' = ignore failures so a stale patch never blocks startup.
ExecStartPre=-__HOME__/.ccgram/apply-all-patches.sh
