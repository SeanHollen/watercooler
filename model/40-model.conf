[Service]
# Fable 5 + max reasoning for every ccgram-spawned Claude Code session.
# On boot, ccgram inherits these and its fresh tmux server passes them to panes.
EnvironmentFile=-/home/seanhollen/.ccgram/model.env
